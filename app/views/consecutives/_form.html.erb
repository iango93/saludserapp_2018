<%= form_with(model: consecutive, local: true) do |form| %>
  <% if consecutive.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(consecutive.errors.count, "error") %> prohibited this consecutive from being saved:</h2>

      <ul>
      <% consecutive.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :suffix %>
    <%= form.text_field :suffix %>
  </div>

  <div class="field">
    <%= form.label :folio_company %>
    <%= form.text_field :folio_company %>
  </div>

  <div class="field">
    <%= form.label :company_id %>
    <%= form.text_field :company_id %>
  </div>

  <div class="field">
    <%= form.label :customer_id %>
    <%= form.text_field :customer_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
