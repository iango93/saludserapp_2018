<%= form_with(model: folio, local: true) do |form| %>
  <% if folio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(folio.errors.count, "error") %> prohibited this folio from being saved:</h2>

      <ul>
      <% folio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :folio %>
    <%= form.text_field :folio %>
  </div>

  <div class="field">
    <%= form.label :headquarter_id %>
    <%= form.text_field :headquarter_id %>
  </div>

  <div class="field">
    <%= form.label :customer_id %>
    <%= form.text_field :customer_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
