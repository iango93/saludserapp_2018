<%= form_with(model: log1, local: true) do |form| %>
  <% if log1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(log1.errors.count, "error") %> prohibited this log1 from being saved:</h2>

      <ul>
      <% log1.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :data %>
    <%= form.text_area :data %>
  </div>

  <div class="field">
    <%= form.label :data_trans %>
    <%= form.text_field :data_trans %>
  </div>

  <div class="field">
    <%= form.label :user_hash %>
    <%= form.text_field :user_hash %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
